{
  "type": "module",
  "scripts": {
    "dev": "npm run build && node scripts/dev-server.mjs",
    "build": "node scripts/build.mjs",
    "lint": "tsc --noEmit",
    "test": "npm run build && node --test tests/teamIsolation.test.mjs tests/customerModule.test.mjs tests/followUps.test.mjs tests/mvpFlow.test.mjs"
  },
  "devDependencies": {
    "typescript": "latest"
  }
}
