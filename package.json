{
  "type": "module",
  "scripts": {
    "dev": "npm run build && python3 -m http.server 5173 -d public",
    "build": "rm -rf dist public && tsc && mkdir -p public && cp index.html public/index.html && cp src/styles/app.css public/app.css && cp -r dist public/dist",
    "lint": "tsc --noEmit",
    "test": "npm run build && node --test tests/teamIsolation.test.mjs"
  },
  "devDependencies": {
    "typescript": "latest"
  }
}
